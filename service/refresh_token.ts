import { MongoClient } from "mongodb";
export const refreshToken = async ({
  client,
  expToken,
}: {
  client: any;
  expToken: String;
}): Promise<String> => {
  let newToken = "";
  try {
    return "";
  } catch {
    return "";
  }
};
